import * as wcfclient from './wcfclient';
import { priority, common } from './nform';

const form = priority.netitems.form;
const metadata = priority.netitems.metadata;
const ngtw = priority.netitems.ngtw;
const shared = priority.netitems.shared;
const ngen = priority.netitems.ngen;

export { wcfclient, priority, common, form, metadata, ngtw, shared, ngen };